%p#notice= notice

%p
  %b Name:
  = @source.name
%p
  %b Url:
  = @source.url
%p
  %b Tags:
  = @source.tagslist

= link_to "Update", "#{source_url}/update_entries", class: "btn btn-primary"
= link_to 'Edit', edit_source_path(@source), class: "btn btn-warning"
= link_to 'Back', sources_path, class: "btn btn-default"

%hr

= render "entries/entries", entries: @source.entries
