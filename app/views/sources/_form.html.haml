= simple_form_for(@source) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :url
    = f.text_field :tagslist, value: @source.tagslist

  .form-actions
    = f.button :submit
