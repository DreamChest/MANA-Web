= simple_form_for(@tag) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name, label: l("words.name")
    = f.input :color, label: l("words.color")

  .form-actions
    = f.button :submit, class: "btn btn-primary"
